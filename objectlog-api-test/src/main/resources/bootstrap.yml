
spring:
  application:
    name: objectlog-api-test
  cloud:
    nacos:
      discovery:
        namespace: public
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        username: nacos
        password: nacos
        namespace: public #命名空间Id public是默认命名空间无编号则为空,当然如访问public可不写
        group: DEFAULT_GROUP #默认分组 如访问 DEFAULT_GROUP 可不写
        file-extension: yml
